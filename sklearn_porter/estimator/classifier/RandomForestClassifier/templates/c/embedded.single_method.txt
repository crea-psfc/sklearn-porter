int {method_name}_{method_id}(float features[], float contributions[]) {{
    int classes[{n_classes}];
    int class_idx = 0;
    int class_val = classes[0];
    int i;
    for (i=0; i < {n_features}; i++) contributions[i] = 0;
    
    {tree_branches}
    for (i = 1; i < {n_classes}; i++) {{
        if (classes[i] > class_val) {{
            class_idx = i;
            class_val = classes[i];
        }}
    }}
    return class_idx;
}}
