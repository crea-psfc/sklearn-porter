float {method_name}_{method_id}(float features[], float contributions[]) {{
    float classes[{n_classes}];
    int i;
    for (i=0; i < {n_classes}; i++) classes[i] = 0;
    for (i=0; i < {n_features}; i++) contributions[i] = 0;
    
    {tree_branches}
    
    return classes[1];
}}
