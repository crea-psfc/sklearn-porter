{methods}
float {method_name} (float features[], float total_contributions[]) {{
    float contributions[{n_features}];
    float class_prob = 0.0;
    int i;
    for (i=0; i < {n_features}; i++) total_contributions[i] = 0;

    {method_calls}

    float a = {n_estimators}.0f;
    return ((float)class_prob)/((float)a);
}}
